#include "minishell.h"
#include <stdio.h>

bool	ms_exec(t_parser *pa, t_info *info)
{
	//fprintf(stderr, "\nms_exec() not implemented!\n");
	(void)pa;
	(void)info;
	return (true);
}

bool	ms_execinit(t_shell *sh)
{
	//printf("ms_exec_init() not implemented!\n");
	(void)sh;
	return (true);
}

